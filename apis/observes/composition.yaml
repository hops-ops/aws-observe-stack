apiVersion: apiextensions.crossplane.io/v1
kind: Composition
metadata:
  name: observes.stacks.aws.hops.ops.com.ai
spec:
  compositeTypeRef:
    apiVersion: stacks.aws.hops.ops.com.ai/v1alpha1
    kind: Observe
  mode: Pipeline
  pipeline:
  - functionRef:
      name: hops-ops-stack-aws-observerender
    step: render
  - functionRef:
      name: crossplane-contrib-function-auto-ready
    step: crossplane-contrib-function-auto-ready
